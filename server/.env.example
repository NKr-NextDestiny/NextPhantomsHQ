# Prisma CLI braucht DATABASE_URL hier (server/).
# Fuer lokales Dev: @localhost. In Docker wird diese Datei durch
# die Umgebungsvariable aus docker-compose.yml ueberschrieben.
DATABASE_URL=postgresql://phantoms:changeme@localhost:5432/next_phantoms_hq
